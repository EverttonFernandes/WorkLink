TASK_KEY=WL-025
FASE=IMPLEMENTACAO

ULTIMA_HIPOTESE:
- Login proprio por email e senha reduz custo inicial sem remover a possibilidade de canais externos futuros.

ULTIMO_RESULTADO:
- Historia, tarefas, criterios de aceite e plano de execucao documentados.
- WL-025 posicionada antes da avaliacao de mensageria WLT-038.
- Suite funcional Jest detectada em functional-tests/src/specs.
- WLT-037 formalmente replanejada para o backlog por depender de acoes manuais de cloud.
- Arquitetura atual de OTP, contas e sessoes mapeada para reaproveitar access/refresh tokens.

GATES_OBSERVADOS:
- QA: PENDING
- MOBILE_UX: PENDING
- SRE: PENDING
- SECURITY: PENDING
- ARCH_REVIEW: PENDING
- FINAL_REVIEW: PENDING

PROXIMA_ACAO_RECOMENDADA:
- Escrever os testes de cadastro, login, bloqueio e recuperacao antes da implementacao backend.

RISCOS_ABERTOS:
- Migracao de contas existentes baseadas em telefone.
- Recuperacao de senha exige canal transacional configurado.
- Celular nao verificado nao pode gerar badge de confianca.
- Canais externos devem permanecer ocultos enquanto desativados.
2026-06-11 - Iteracao 2: 323 testes backend passaram, mas o gate JaCoCo reprovou com 92% (minimo 95%).
Hipotese/correcao: cobrir persistencia JDBC local, regras do dominio, mailbox e endpoint de suporte sem reduzir o gate.
2026-06-12 - Iteracao 3: correcoes de seguranca e operacao aplicadas na autenticacao local.
- Cadastro publico passou a rejeitar telefone legado ja existente para evitar takeover por simples conhecimento do numero.
- Suporte de token de recuperacao ficou restrito a `local/test`, default false no compose e sem exposicao em runtime normal.
- Reset de senha e rotacao de refresh token agora dependem de consumo/revogacao atomicos dentro de transacao.
- Adapter SMTP opcional foi introduzido para producao, mantendo local e testes desacoplados do provedor.
- Validacao completa em Docker segue bloqueada porque o daemon do Docker Desktop nao esta acessivel neste WSL.
2026-06-14 - Iteracao 4: backend e fluxo funcional da WL-025 foram revalidados com sucesso.
- `mvn -q test` e `mvn -q verify` passaram no backend; Failsafe confirmou a migracao `V022` em PostgreSQL real.
- Cobertura backend recalculada em `worklink-api/target/site/jacoco/jacoco.csv`: 95.18% de instrucoes e 95.93% de linhas.
- Suite funcional `jest --runInBand` passou com 17 cenarios cobrindo cadastro, login, refresh, logout e recuperacao.
- Causa raiz do falso 404 funcional encontrada: os targets full-stack do `Makefile` nao reconstruíam o `worklink-api` e podiam usar imagem stale.
- O `Makefile` foi ajustado para usar `up -d --build --wait worklink-api` nos alvos full-stack que dependem do backend atual.
- `flutter analyze`, goldens de autenticacao local e `flutter test test/unit` passaram antes do novo erro externo de guest-services no Docker Desktop.
- Revalidacao final de `flutter test test/widget` ficou pendente por falha WSL/Docker (`ubuntu.sock` ausente na montagem), nao por erro de codigo diagnosticado.
2026-06-14 - Iteracao 5: gate mobile final da WL-025 ficou verde.
- `flutter test test/widget` passou integralmente apos a recuperacao do Docker Desktop.
- O unico ajuste necessario foi alinhar o teste de autenticacao concluida ao texto atual da tela: `Conta autenticada`.
- A historia agora esta com backend, funcional, analyze, goldens, unitarios mobile e widgets validados localmente.
