# WLT-029 — Progress Log

**Status**: DOING

## Timeline

### Fresh Context 1 — 2026-05-21

- [x] Ralph Loop invocado explicitamente pelo usuário.
- [x] Confirmado que `docs/tasks/WLT-029/IMPLEMENTATION.md` e `progress.txt` não existiam.
- [x] Workflow `.agents/workflows/start-work.md` citado pela skill não existe neste repositório; artefatos foram criados manualmente seguindo `ralph-loop/execution-plan`.
- [x] Functional Test Discovery: infraestrutura funcional detectada em `functional-tests/src/specs` com Jest.
- [x] CI anterior da WLT-029 validada:
  - [x] `26197769468`: success
  - [x] `26198971069`: success
- [x] Ambiente local verificado:
  - [x] `docker`: inicialmente indisponível no WSL; depois confirmado via Docker Desktop/`docker.exe`
  - [x] `flutter`: indisponível diretamente no WSL atual
- [x] `gh variable list` e `gh secret list` não retornaram `WORKLINK_HOMOLOGATION_API_BASE_URL`.
- [x] Product Manager subagente confirmou WLT-029 como próxima elegível e sem bloqueio de produto.
- [x] SRE subagente confirmou CI verde, mas artifact homologation pulado por falta de URL.
- [x] Security Specialist reprovou o fluxo promovível por APK debug, assinatura debug, URL sem validação rígida e APK binário no git.
- [x] QA confirmou que o script de promoção falha com `gh run view --json artifacts` na CLI atual.
- [ ] Obter URL de backend de homologação acessível pelo Android físico ou habilitar ambiente local com Docker/Flutter.
- [ ] Gerar artifact `worklink-android-homologation-<commit>`.
- [ ] Promover APK como asset do GitHub Release e metadados para `artifacts/homologation/releases/<versao>/android`.
- [ ] Validar instalação manual no Android físico.
- [ ] Fechar WLT-029 com commit e tag semântica no mesmo hash.

### Fresh Context 2 — 2026-05-21

- [x] Correção iniciada para atender Segurança/QA:
  - [x] `mobile-android-homologation-candidate` passa a exigir URL HTTPS pública/permitida.
  - [x] Build promovível passa de `debug` para `release`.
  - [x] Gradle passa a usar signing config de homologação via secrets.
  - [x] APK de homologação deixa de ser gravado como binário no git; fica como asset de GitHub Release.
  - [x] Script de promoção passa a descobrir artifacts pela GitHub API.
  - [x] Criado target separado para APK local full-stack debug, sem promoção.
- [x] Segunda revisão de Segurança reprovou pontos adicionais e eles foram endereçados:
  - [x] allowlist de host passou a ser obrigatória.
  - [x] parser de URL passou a rejeitar IPv6, localhost, `.local`, IP privado/local/reservado.
  - [x] promoção passou a validar o certificado real do APK com `apksigner`.
  - [x] promoção passou a exigir `WORKLINK_ANDROID_HOMOLOGATION_CERT_SHA256`.
- [x] Terceira revisão de Segurança reprovou revalidação ausente na promoção e colisão de nome do asset; ambos foram corrigidos.
- [x] Quarta revisão de Segurança aprovou o patch de endurecimento.
- [x] Validações rápidas passaram:
  - [x] `sh -n` nos scripts alterados.
  - [x] `validate_homologation_api_base_url.sh` aceita HTTPS público.
  - [x] `validate_homologation_api_base_url.sh` rejeita HTTP/local e host fora da allowlist.
  - [x] `make -n` cobre target homologation release e target local debug.
  - [x] `git diff --check`.
- [x] Teste fake de promoção com `gh`/`apksigner` simulados comprovou que somente metadados/checksum/ponteiro são gravados em `artifacts/homologation/releases`, sem APK binário.
- [x] `make -n` validou URL com query string após quoting dos `dart-define` e metadados.
- [ ] `make DOCKER=docker.exe mobile-static-analysis` foi tentado, mas interrompido porque o pull inicial da imagem Flutter ficou lento demais para a janela atual.

## Bloqueio Atual

Falha observada:

- Não há URL de backend de homologação configurada no GitHub Actions.
- Não há allowlist `WORKLINK_HOMOLOGATION_ALLOWED_HOSTS` configurada no GitHub Actions.
- Não há secrets de assinatura Android de homologação configurados no GitHub Actions.
- Não há fingerprint `WORKLINK_ANDROID_HOMOLOGATION_CERT_SHA256` configurado no GitHub Actions.
- Flutter não está disponível diretamente no WSL atual; Docker Desktop está acessível via `docker.exe`.

Hipótese de correção:

- Rota preferencial: usar workflow manual com input `homologation_api_base_url` apontando para backend HTTPS acessível pelo celular, com allowlist, signing secrets e fingerprint configurados.
- Rota alternativa para teste rápido local: subir `make homologation-local-up` e gerar APK debug com `MOBILE_LOCAL_API_BASE_URL=http://<ip-da-maquina>:8080 make mobile-android-local-fullstack-candidate`. Esse APK não é promovível.

Ação tomada:

- Pipeline ajustada previamente para aceitar URL por variable, secret ou input manual.
- Pipeline endurecida para exigir assinatura de homologação e validar URL antes de gerar artifact promovível.
- State machine do Ralph Loop criada para rastrear o fechamento.

Resultado:

- WLT-029 permanece em execução até existir APK full-stack versionado e validado.
